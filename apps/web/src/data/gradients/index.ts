/**
 * Gradient catalogue — now published in `adysre/gradients`.
 *
 * See `@/lib/palettes/color` for why this is an alias rather than a copy.
 */

export {
  ALL_GRADIENT_TAGS,
  GRADIENTS,
  GRADIENT_COUNT,
  getGradient,
  similarGradients,
  type Gradient,
  type GradientStop,
  type GradientType,
} from 'adysre/gradients';
