/**
 * Texture catalogue — now published in `adysre/textures`.
 *
 * See `@/lib/palettes/color` for why this is an alias rather than a copy.
 */

export {
  ALL_TEXTURE_TAGS,
  TEXTURES,
  TEXTURE_COUNT,
  getTexture,
  similarTextures,
  type Texture,
  type TextureType,
} from 'adysre/textures';
