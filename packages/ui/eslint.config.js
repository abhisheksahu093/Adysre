import react from '@adysre/config/eslint/react';

/**
 * `adysre` is a published, framework-free React library — it needs the React
 * Hooks rules but must not be pushed towards Next.js-only APIs. See
 * `packages/config/eslint/react.js`.
 */
export default react;
