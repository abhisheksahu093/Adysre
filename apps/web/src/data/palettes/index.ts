/**
 * Palette catalogue — now published in `adysre/palettes`.
 *
 * See `@/lib/palettes/color` for why this is an alias rather than a copy.
 */

export {
  ALL_PALETTE_TAGS,
  PALETTES,
  PALETTE_COUNT,
  getPalette,
  similarPalettes,
  type Palette,
} from 'adysre/palettes';
