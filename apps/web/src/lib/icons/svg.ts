/**
 * Icon SVG builders — now published in `adysre/icons`.
 *
 * See `@/lib/palettes/color` for why this is an alias rather than a copy.
 */

export {
  DEFAULT_ICON_STYLE,
  ICON_COPY_FORMATS,
  ICON_SIZES,
  buildCssRule,
  buildDataUri,
  buildJsxSnippet,
  buildReactComponent,
  buildSvgMarkup,
  formatIcon,
  humanize,
  pascalCase,
  slugify,
  toJsxAttrs,
  type IconCopyFormat,
  type IconCopyFormatId,
  type IconStyle,
} from 'adysre/icons';
