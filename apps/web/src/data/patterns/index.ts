/**
 * Pattern catalogue — now published in `adysre/patterns`.
 *
 * See `@/lib/palettes/color` for why this is an alias rather than a copy.
 */

export {
  ALL_PATTERN_TAGS,
  PATTERNS,
  PATTERN_COUNT,
  getPattern,
  similarPatterns,
  type Pattern,
  type PatternType,
} from 'adysre/patterns';
