/**
 * Gradient CSS builders — now published in `adysre/gradients`.
 *
 * See `@/lib/palettes/color` for why this is an alias rather than a copy.
 */

export {
  GRADIENT_FORMATS,
  downloadGradientPng,
  formatGradient,
  gradientToCss,
  gradientToStyle,
  type GradientFormat,
  type GradientFormatId,
} from 'adysre/gradients';
